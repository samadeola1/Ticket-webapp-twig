{# templates/partials/_navbar.html.twig #}
<nav class="fixed w-full bg-white shadow z-50">
  <div class="max-w-[1440px] mx-auto flex items-center justify-between px-6 py-4">
    <a href="{{ path('app_landing') }}" class="text-2xl font-bold text-indigo-600 hover:text-indigo-700 transition">
      Ticketly
    </a>

    <div class="hidden md:flex flex-1 justify-center items-center space-x-6">
      <a href="{{ path('app_landing') }}" class="hover:text-indigo-600 transition font-medium">Home</a>
      <a href="{{ path('app_dashboard') }}" class="hover:text-indigo-600 transition font-medium">Dashboard</a>
      <a href="{{ path('app_tickets') }}" class="hover:text-indigo-600 transition font-medium">Tickets</a>
    </div>

    <div class="hidden md:flex items-center space-x-4">
      <div id="nav-user-links" class="hidden items-center space-x-4">
        <span id="nav-user-name" class="text-gray-700 font-medium"></span>
        <button id="nav-logout-btn" class="px-4 py-2 bg-red-500 text-white rounded-md hover:bg-red-600 transition">
          Logout
        </button>
      </div>
      <div id="nav-guest-links" class="hidden items-center space-x-4">
        <a href="{{ path('app_login') }}" class="px-4 py-2 border border-indigo-600 text-indigo-600 rounded-md hover:bg-indigo-50 transition-transform hover:scale-105">
          Login
        </a>
        <a href="{{ path('app_signup') }}" class="px-4 py-2 bg-indigo-600 text-white rounded-md hover:bg-indigo-700 transition-transform hover:scale-105 shadow">
          Get Started
        </a>
      </div>
    </div>

    <button id="mobile-menu-btn" class="md:hidden text-gray-700 focus:outline-none">
      <i data-lucide="menu" class="w-6 h-6"></i>
    </button>
  </div>

  <div id="mobile-menu" class="hidden md:hidden bg-white shadow-lg">
    <a href="{{ path('app_landing') }}" class="block px-6 py-3 border-b hover:bg-indigo-50">Home</a>
    <a href="{{ path('app_dashboard') }}" class="block px-6 py-3 border-b hover:bg-indigo-50">Dashboard</a>
    <a href="{{ path('app_tickets') }}" class="block px-6 py-3 border-b hover:bg-indigo-50">Tickets</a>

    <div id="mobile-nav-user-links" class="hidden">
        <span id="mobile-nav-user-name" class="block px-6 py-3 border-b"></span>
        <button id="mobile-nav-logout-btn" class="w-full text-left px-6 py-3 border-b text-red-500">Logout</button>
    </div>
    <div id="mobile-nav-guest-links" class="hidden">
        <a href="{{ path('app_login') }}" class="block px-6 py-3 border-b text-indigo-600">Login</a>
        <a href="{{ path('app_signup') }}" class="block px-6 py-3 border-b text-white bg-indigo-600">Get Started</a>
    </div>
  </div>
</nav>